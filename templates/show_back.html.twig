{% extends 'back.html.twig' %}

{% block body %}
    <section class="restaurants">
        <restaurant>
            <h2> {{ restaurant.nom }} </h2>
            <div class"content">
            {{ restaurant.position }}
            {{ restaurant.dateEntrer | date('d/m/Y')}}
                Ayant pour proprietaire : {{ restaurant.gerantRestaurant }}
            </div>
            <div class"content">
                <img src="{{ asset('/images/banana.jpg') }}" alt="2">
            </div>
        </restaurant>
    </section>
{% endblock %}